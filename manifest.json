{
  "manifest_version": 3,
  "name": "Job URL Redirector",
  "version": "1.0",
  "description": "Redirects /job URLs to /ssr-job",
  "permissions": ["tabs", "storage"],
  "background": {
    "service_worker": "background.js"
  },
  "action": {}
}
